
function sumTwo(add, multi) {

    function add_em(add) {
        let adding = add + 5
        return adding
    }
    console.log(add_em(600))

    function multi(mult) {
        let mul = mult * 10
        return mul
    }
    console.log(multi(10))

    Total = add + multi;
    return Total
}

console.log(605 + 100)